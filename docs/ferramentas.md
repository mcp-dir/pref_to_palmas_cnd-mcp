# Ferramentas

Prefeitura TO Palmas: Certidão Negativa de Débitos expõe 1 ferramenta (todas somente leitura).

### 1. `pref_to_palmas_cnd_consultar`
**Input**: `cpf` (opcional), `cnpj` (opcional), `finalidade` (opcional), `outra` (opcional)

Prefeitura TO Palmas: Certidão Negativa de Débitos, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Prefeitura TO Palmas: Certidão Negativa de Débitos
```
